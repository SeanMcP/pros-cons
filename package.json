{
  "name": "pros-cons",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "build": "NODE_ENV=production postcss styles.css -o build/compiled.css && esbuild main.ts --bundle --minify --outfile=build/bundle.js && cp index.html build/index.html",
    "dev:postcss": "postcss styles.css -o compiled.css",
    "dev:bundle": "esbuild main.ts --bundle --sourcemap --outfile=bundle.js --watch",
    "dev:serve": "reload -b -p 3039",
    "start": "npm-run-all --parallel dev:*",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^10.2.4",
    "esbuild": "^0.11.5",
    "npm-run-all": "^4.1.5",
    "postcss": "^8.2.6",
    "postcss-cli": "^8.3.1",
    "reload": "^3.1.1",
    "tailwindcss": "^2.0.3"
  },
  "dependencies": {}
}
